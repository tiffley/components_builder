docker-compose up -d
docker-compose down
docker-compose logs trino

-- trino
http://localhost:8080
just login by input any user name

java -jar -Duser.timezone=UTC trino.jar --server localhost:8080 --catalog mysql --schema db1

-- mysql
mysql -h 127.0.0.1 -P 3306 -u root -p
pw

