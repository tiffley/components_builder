docker-compose up -d
docker-compose down
docker-compose logs grafana

http://localhost:3000
admin / pw

add datasource

Host URL - mysql:3306
Database name - db1
Username - root
Password - pw

save, then explore data

choose a table and 2 columns, then run query. graph will be created


-- mysql
mysql -h 127.0.0.1 -P 3306 -u root -p
pw
